# 更新日志

本项目的所有重要变更记录在此文件。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## v0.1.0（2026-09-05）

首个功能完整版本。**数据与展示分离架构**：兑换码数据由姊妹仓库 [game-codes-repo](https://github.com/Ecila01/game-codes-repo) 手工维护，本挂件只读 raw 直链，无需任何服务端。

### 新增

- **挂件双形态 UI**：收起态「大肥鱼」贴纸——气泡内显示最新兑换码摘要、悬停查看最新 3 条、按压 Q 弹动画；双击展开完整列表
- **展开面板**：4 款游戏（原神/星穹铁道/鸣潮/绝区零）分组可折叠；每条码一键复制（"已复制✓"反馈）；右键本地标记"已兑换 / 已过期"（仅存本地，重启保留）；过期码置灰删除线
- **多源数据拉取**：GitHub raw 直链 + jsDelivr 镜像按序回退；ETag 条件请求省流量；10s 超时 + 3 次退避重试；全部源失败自动沿用本地缓存并提示"数据暂时无法更新"
- **系统通知**：发现新兑换码弹 Windows Toast（最多列 3 个码 + 省略号）；托盘菜单内置"测试通知"；失败提醒每天最多一次防打扰
- **每日提醒**：当日首次启动或每日固定时间（默认 12:00）自动检查更新，当日去重
- **定时轮询**：默认 30 分钟，可在设置页调整（1~1440 分钟）
- **拖拽 + 贴边吸附**：挂件自由拖动，靠近屏幕四边自动吸附（150ms 动画）
- **系统托盘**：右键菜单（显示/隐藏、立即刷新、测试通知、打开数据仓库、开机自启动、设置、退出）；双击托盘切换挂件；点挂件 × 隐藏到托盘
- **设置页**：数据源启停/增删/排序、轮询间隔、每日时间、通知开关、界面（缩放 0.6~2.5 倍/透明度/置顶/吸附）、开机自启动——**保存即热生效，无需重启**
- **开机自启动**：HKCU 注册表 Run 键，无需管理员；托盘菜单与设置页状态双向同步
- **挂件右键菜单**：右键小肥鱼或面板空白处，弹出与托盘一致的菜单（刷新/设置/自启动/退出等）
- **面板自由调整大小**：拖动展开面板右下角尺寸角托自由调整长宽，尺寸自动记忆（写入 `ui.expanded_size`）
- **鱼与气泡素材分离**：本体裁剪为纯人物贴图（最大连通域过滤，像素级去除气泡残留），气泡改为代码绘制（白底深蓝描边+引导圆点），隐藏时仅显示鱼本体；收起态为"气泡列+本体列"布局，气泡永不遮挡人物（仿 Dsh 大肥鱼挂件 v0.2.5）
- **徽章/分组重配色**：按 WCAG AA（正文 ≥4.5:1）重新设计深色主题配色——徽章改浅色文字+同色相低透明底+细描边，修复 Qt 8 位 hex（#AARRGGBB）解析差异导致的串色

### 打包

- 便携版单文件 `DD RedeeMe.exe`（PyInstaller onefile，双击即用，配置生成在 exe 旁）
- 安装包 `DD RedeeMe_0.1.0_x64-setup.exe`（Inno Setup：每用户安装免管理员、可选桌面快捷方式与开机自启、卸载保留本地缓存与标记、快捷方式携带 AppUserModelID 保证 Toast 归属正确）
- 一键构建：`tools\build_release.bat`（装依赖 → 生成图标/抠图素材 → PyInstaller → Inno Setup → 组装 release）

### 已知限制

- 便携版（无开始菜单快捷方式）的 Toast 横幅在 Win11 可能不弹或不进通知中心；**安装版无此问题**（快捷方式已携带 AppUserModelID）
- onefile 版启动需自解压约 1~3 秒；个别杀毒软件对 PyInstaller 产物有误报，可加入白名单或改从源码运行
- 兑换码是否过期由数据维护者标记（`expired` 字段）+ 用户本地标记，程序不按日期自动推断
